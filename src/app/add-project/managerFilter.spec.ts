import { managerFilterPipe } from './managerFilter';

describe('managerFilterPipe', () => {
  it('create an instance managerFilterPipe', () => {
    const pipe = new managerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});