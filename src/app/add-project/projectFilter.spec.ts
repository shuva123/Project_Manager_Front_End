import { projectFilterPipe } from './projectFilter';

describe('projectFilterPipe', () => {
  it('create an instance projectFilterPipe', () => {
    const pipe = new projectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});